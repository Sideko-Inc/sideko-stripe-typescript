import Stripe, { Environment } from "@sideko-inc/stripe";

describe("tests client.subscriptionSchedule.list", () => {
  test.concurrent(
    "GET /v1/subscription_schedules | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.subscriptionSchedule.list().asResponse(),
        client.subscriptionSchedule.list(),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.subscriptionSchedule.get", () => {
  test.concurrent(
    "GET /v1/subscription_schedules/{schedule} | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.subscriptionSchedule.get({ schedule: "string" }).asResponse(),
        client.subscriptionSchedule.get({ schedule: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.subscriptionSchedule.create", () => {
  test.concurrent(
    "POST /v1/subscription_schedules | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.subscriptionSchedule.create().asResponse(),
        client.subscriptionSchedule.create(),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.subscriptionSchedule.update", () => {
  test.concurrent(
    "POST /v1/subscription_schedules/{schedule} | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.subscriptionSchedule.update({ schedule: "string" }).asResponse(),
        client.subscriptionSchedule.update({ schedule: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.subscriptionSchedule.cancel", () => {
  test.concurrent(
    "POST /v1/subscription_schedules/{schedule}/cancel | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.subscriptionSchedule.cancel({ schedule: "string" }).asResponse(),
        client.subscriptionSchedule.cancel({ schedule: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.subscriptionSchedule.release", () => {
  test.concurrent(
    "POST /v1/subscription_schedules/{schedule}/release | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.subscriptionSchedule
          .release({ schedule: "string" })
          .asResponse(),
        client.subscriptionSchedule.release({ schedule: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});
