import Stripe, { Environment } from "@sideko-inc/stripe";

describe("tests client.paymentMethodConfiguration.list", () => {
  test.concurrent(
    "GET /v1/payment_method_configurations | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.paymentMethodConfiguration.list().asResponse(),
        client.paymentMethodConfiguration.list(),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.paymentMethodConfiguration.get", () => {
  test.concurrent(
    "GET /v1/payment_method_configurations/{configuration} | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.paymentMethodConfiguration
          .get({ configuration: "string" })
          .asResponse(),
        client.paymentMethodConfiguration.get({ configuration: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.paymentMethodConfiguration.create", () => {
  test.concurrent(
    "POST /v1/payment_method_configurations | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.paymentMethodConfiguration.create().asResponse(),
        client.paymentMethodConfiguration.create(),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.paymentMethodConfiguration.update", () => {
  test.concurrent(
    "POST /v1/payment_method_configurations/{configuration} | testId: generated_success | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Stripe({
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.paymentMethodConfiguration
          .update({ configuration: "string" })
          .asResponse(),
        client.paymentMethodConfiguration.update({ configuration: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});
